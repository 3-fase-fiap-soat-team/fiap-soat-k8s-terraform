name: Deploy EKS Infrastructure

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  terraform-eks:
    name: Provision EKS Cluster
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write
      id-token: write

    env:
      AWS_REGION: ${{ secrets.AWS_DEFAULT_REGION }}
      EKS_CLUSTER_NAME: fiap-soat-eks-dev

    defaults:
      run:
        working-directory: environments/dev

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.5

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ secrets.AWS_DEFAULT_REGION }}
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}

      - name: Terraform Init
        run: terraform init

      - name: Terraform Format
        run: terraform fmt -check
        continue-on-error: true

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        if: github.event_name == 'pull_request'
        run: terraform plan -input=false
        
      - name: Terraform Apply
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: terraform apply -auto-approve -input=false
        continue-on-error: true
        
      - name: Terraform Apply Status
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          echo "⚠️  Terraform apply pode falhar se o cluster já existir (sem backend S3)"
          echo "✅ Continuando para deploy dos manifestos Kubernetes..."

  deploy-k8s-manifests:
    name: Deploy Kubernetes Manifests
    runs-on: ubuntu-latest
    needs: terraform-eks
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    permissions:
      contents: read
      id-token: write

    env:
      AWS_REGION: ${{ secrets.AWS_DEFAULT_REGION }}
      EKS_CLUSTER_NAME: fiap-soat-eks-dev

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ secrets.AWS_DEFAULT_REGION }}
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}

      - name: Wait for EKS Cluster
        run: |
          echo "⏳ Aguardando cluster EKS ficar disponível..."
          aws eks wait cluster-active --name $EKS_CLUSTER_NAME --region $AWS_REGION || true
          sleep 30

      - name: Configure kubectl
        run: |
          aws eks update-kubeconfig --region $AWS_REGION --name $EKS_CLUSTER_NAME
          kubectl version --client
          kubectl cluster-info

      - name: Deploy Kubernetes Manifests
        run: |
          echo "📦 Aplicando manifests de infraestrutura..."
          echo "ℹ️  Deployment é gerenciado pelo repositório da aplicação"
          cd manifests
          kubectl apply -f namespace.yaml
          kubectl apply -f configmap.yaml
          
          # Criar secret dinamicamente via GitHub Secrets
          echo "🔐 Criando secret..."
          DB_PASS="${{ secrets.DB_PASSWORD }}"
          JWT_SEC="${{ secrets.JWT_SECRET }}"
          
          # Usar valores padrão se secrets não estiverem configurados (apenas dev)
          DB_PASS="${DB_PASS:-SuperSecret123!}"
          JWT_SEC="${JWT_SEC:-default-jwt-secret-change-me-in-production}"
          
          kubectl create secret generic fiap-soat-application-secrets \
            --namespace=fiap-soat-app \
            --from-literal=DATABASE_PASSWORD="$DB_PASS" \
            --from-literal=JWT_SECRET="$JWT_SEC" \
            --dry-run=client -o yaml | kubectl apply -f -
          
          kubectl apply -f service.yaml
          kubectl apply -f hpa.yaml
          echo ""
          echo "✅ Manifests de infraestrutura aplicados com sucesso!"

      - name: Verify Infrastructure
        run: |
          echo ""
          echo "=== � Status do Service ==="
          kubectl get svc -n fiap-soat-app fiap-soat-application-service
          echo ""
          echo "=== � ConfigMap ==="
          kubectl get configmap -n fiap-soat-app fiap-soat-application-config
          echo ""
          echo "=== � Secret ==="
          kubectl get secret -n fiap-soat-app fiap-soat-application-secrets
          echo ""
          echo "=== 🌐 Service Endpoints ==="
          kubectl get endpoints -n fiap-soat-app fiap-soat-application-service || echo "⚠️  Endpoints não disponíveis (deployment ainda não criado)"
          echo ""
          echo "ℹ️  O deployment da aplicação será gerenciado pelo repositório fiap-soat-application"
